{
  "name": "jijian-parking",
  "version": "2.0.0",
  "description": "3D Voxel parking game with World App integration and tokenomics",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "ethers": "^6.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
